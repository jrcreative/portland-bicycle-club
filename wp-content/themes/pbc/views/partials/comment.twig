<div class="blog-comment {{comment.comment_type}}" id="blog-comment-{{comment.ID}}">
    <div><div>{{ fn('get_avatar', comment.author.id, 50) }} <span class="comment-author">{{comment.author.name}} says</span></div>
    <div class="comment-content">{{comment.comment_content|wpautop}}</div><div><i>{{ comment.date }}</i></div></div>

    <section class="comment-box">
        <!-- child comments -->
        {% if post.comments %}
            <div class="comments">
                {% for cmt in comment.children %}
                    {% include "partials/comment.twig" with {comment:cmt} %}
                {% endfor %}
            </div>
        {% endif %}
    </section>
</div>