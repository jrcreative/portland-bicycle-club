<section class="membership bg-{{ row.style }}">
    <div class="row column">
        {% apply shortcodes %}
            [pwtc_member_renew_nag {{ row.attributes }}]
        {% endapply %}
    </div>
</section>